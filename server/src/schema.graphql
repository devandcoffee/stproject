type User {
  id: Int!
  email: String!
}

type Query {
  user(id: Int!): User!
  users: [User!]!
}

type Mutation {
  login(email: String!, password: String!): Boolean
  createUser(email: String!, password: String!): Boolean
}
